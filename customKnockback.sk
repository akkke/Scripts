on damage:
    wait 1 tick
    event is not cancelled
    if (y of victim's velocity) < 2:
        set y of victim's velocity to (y of victim's velocity + 0.4)
        push victim in attacker's horizontal direction with force 0.5